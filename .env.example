# ---------------------------------------------------------
# Inoot — Environment Variables
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local — it is already in .gitignore.
# ---------------------------------------------------------

# PostgreSQL connection string
# Local dev:    postgresql://username:password@localhost:5432/inoot
# Production:   use your Neon / Supabase / Railway connection string
DATABASE_URL=your_postgresql_connection_string

# Anthropic Claude API key (server-side only)
# Get yours at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-your_key_here

# NextAuth secret — generate with: openssl rand -base64 64
NEXTAUTH_SECRET=generate_a_random_64_char_string

# The public URL of your app
# Local dev:    http://localhost:3000
# Production:   https://your-app.vercel.app
NEXTAUTH_URL=http://localhost:3000
